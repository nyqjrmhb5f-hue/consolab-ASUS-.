# SERVER

Runtime host layer for ConsoleLab.

This module is the service chassis for HTTP transport, sockets, health surfaces, and internal runtime configuration that support the gateway and agent broker.
