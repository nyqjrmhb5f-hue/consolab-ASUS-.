# TUNNELS

Ephemeral secure transport paths.

This module is the wormhole layer for `SYNAPSE-BRIDGE`. Tunnel definitions here should be short-lived, authenticated, and tightly coupled to session control and audit records.
