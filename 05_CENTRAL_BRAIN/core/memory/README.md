# MEMORY

Persistent Memory Module.

This module stores long-lived operator context, approved session memory, and durable state that should survive across agent interactions without leaking into public interfaces.
