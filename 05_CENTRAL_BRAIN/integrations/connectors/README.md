# Connector Inventory

This directory stores the real MCP connector inventory and room-facing connector manifests for `MCP-BRAIN`.

The source of truth is the active connector/session inventory, not raw API keys.
